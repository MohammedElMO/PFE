// import { InfoToast } from "./notifications/toaster-notifier";
import "./style.css"
import "./utils/drop-down-controle"
import "./utils/logIn-out"
// import { icon, library } from "@fortawesome/fontawesome-svg-core"

// const lib = library.add(faFirefoxBrowser)

// const MapBtn = document.querySelector("#map-btn") as HTMLAnchorElement

// MapBtn.innerHTML = icon({
//   prefix: faFirefoxBrowser.prefix,
//   iconName: faFirefoxBrowser.iconName,
// }).html.join("")


// const observeUrlChange = () => {
//     let oldHref = document.location.href;
//     const observer = new MutationObserver(mutations => {
//       if (oldHref !== window.location.href) {
//         oldHref = window.location.href;
//         /* Changed ! your code here */
//         console.log('dafadf')
//         console.log(mutations)
//         InfoToast("you have been signed out","info","center")
//       }
//     });
//     observer.observe(document, { childList: true, subtree: true });
//   };
  
//   document.addEventListener("load" ,observeUrlChange)
